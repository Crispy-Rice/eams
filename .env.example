# .env 模板 —— 复制为 .env 后填入真实值（.env 本身已被 .gitignore 忽略，不会入库）
# 字段对应关系见 com/wanhe/common/config.py

# 数据库
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=school_db
DB_CHARSET=utf8mb4
